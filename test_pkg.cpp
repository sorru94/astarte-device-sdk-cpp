#include <astarte_device_sdk/device.hpp>
// Include a header that might rely on the proto definitions to ensure they are found

int main() {
    // Just minimal instantiation to check linking
    return 0;
}
